language: go
go:
  - 1.11.x
  
script:
  - go test ./...

services:
  - mysql
  - postgresql
  - rabbitmq

before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS db_test;'

before_script:
  - psql -c 'CREATE DATABASE db_test;' -U postgres
